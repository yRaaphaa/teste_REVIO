def remove_zeros(num):
    return int("".join(char for char in str(num) if char != '0'))

while True:
    m, n = map(int, input().split())
    if m == 0 and n == 0:
        break
    print(remove_zeros(m + n))
